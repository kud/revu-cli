[tools]
bun = "1.3.11"

[tasks.run]
run = "bun {{config_root}}/index.ts"
description = "Run revu — optionally pass a repo path: mise run run -- /path/to/repo"

[tasks.build]
run = "bun build index.ts --compile --outfile revu-bin"
description = "Compile revu to a standalone binary"
