[tools]
bun = "1.3.11"

[tasks.run]
run = "bun index.ts"
description = "Run revu on the current directory"

[tasks.build]
run = "bun build index.ts --compile --outfile revu-bin"
description = "Compile revu to a standalone binary"
